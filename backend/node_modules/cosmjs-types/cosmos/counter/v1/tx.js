"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MsgClientImpl = exports.MsgIncreaseCountResponse = exports.MsgIncreaseCounter = exports.protobufPackage = void 0;
/* eslint-disable */
const binary_1 = require("../../../binary");
const helpers_1 = require("../../../helpers");
exports.protobufPackage = "cosmos.counter.v1";
function createBaseMsgIncreaseCounter() {
    return {
        signer: "",
        count: BigInt(0),
    };
}
exports.MsgIncreaseCounter = {
    typeUrl: "/cosmos.counter.v1.MsgIncreaseCounter",
    encode(message, writer = binary_1.BinaryWriter.create()) {
        if (message.signer !== "") {
            writer.uint32(10).string(message.signer);
        }
        if (message.count !== BigInt(0)) {
            writer.uint32(16).int64(message.count);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof binary_1.BinaryReader ? input : new binary_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgIncreaseCounter();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.signer = reader.string();
                    break;
                case 2:
                    message.count = reader.int64();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        const obj = createBaseMsgIncreaseCounter();
        if ((0, helpers_1.isSet)(object.signer))
            obj.signer = String(object.signer);
        if ((0, helpers_1.isSet)(object.count))
            obj.count = BigInt(object.count.toString());
        return obj;
    },
    toJSON(message) {
        const obj = {};
        message.signer !== undefined && (obj.signer = message.signer);
        message.count !== undefined && (obj.count = (message.count || BigInt(0)).toString());
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgIncreaseCounter();
        message.signer = object.signer ?? "";
        if (object.count !== undefined && object.count !== null) {
            message.count = BigInt(object.count.toString());
        }
        return message;
    },
};
function createBaseMsgIncreaseCountResponse() {
    return {
        newCount: BigInt(0),
    };
}
exports.MsgIncreaseCountResponse = {
    typeUrl: "/cosmos.counter.v1.MsgIncreaseCountResponse",
    encode(message, writer = binary_1.BinaryWriter.create()) {
        if (message.newCount !== BigInt(0)) {
            writer.uint32(8).int64(message.newCount);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof binary_1.BinaryReader ? input : new binary_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMsgIncreaseCountResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    message.newCount = reader.int64();
                    break;
                default:
                    reader.skipType(tag & 7);
                    break;
            }
        }
        return message;
    },
    fromJSON(object) {
        const obj = createBaseMsgIncreaseCountResponse();
        if ((0, helpers_1.isSet)(object.newCount))
            obj.newCount = BigInt(object.newCount.toString());
        return obj;
    },
    toJSON(message) {
        const obj = {};
        message.newCount !== undefined && (obj.newCount = (message.newCount || BigInt(0)).toString());
        return obj;
    },
    fromPartial(object) {
        const message = createBaseMsgIncreaseCountResponse();
        if (object.newCount !== undefined && object.newCount !== null) {
            message.newCount = BigInt(object.newCount.toString());
        }
        return message;
    },
};
class MsgClientImpl {
    rpc;
    constructor(rpc) {
        this.rpc = rpc;
        this.IncreaseCount = this.IncreaseCount.bind(this);
    }
    IncreaseCount(request) {
        const data = exports.MsgIncreaseCounter.encode(request).finish();
        const promise = this.rpc.request("cosmos.counter.v1.Msg", "IncreaseCount", data);
        return promise.then((data) => exports.MsgIncreaseCountResponse.decode(new binary_1.BinaryReader(data)));
    }
}
exports.MsgClientImpl = MsgClientImpl;
//# sourceMappingURL=tx.js.map